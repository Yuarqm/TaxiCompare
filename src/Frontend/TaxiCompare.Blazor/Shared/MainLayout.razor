@inherits LayoutComponentBase
@inject IAuthService AuthService
@inject NavigationManager Nav

<div class="app-wrapper">
    <nav class="navbar">
        <div class="navbar-inner">
            <a href="/" class="brand">
                <svg width="32" height="32" viewBox="0 0 48 48" fill="none">
                    <rect width="48" height="48" rx="14" fill="#00D46A"/>
                    <path d="M12 30L20 18L28 26L36 16" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
                    <circle cx="36" cy="16" r="3" fill="white"/>
                </svg>
                <span class="brand-name">Такси<span>Сравни</span></span>
            </a>

            <button class="burger @(menuOpen ? "open" : "")" @onclick="ToggleMenu" aria-label="Меню">
                <span></span>
                <span></span>
                <span></span>
            </button>

            <div class="nav-links @(menuOpen ? "nav-open" : "")">
                <a href="/" class="nav-link" @onclick="CloseMenu">Поиск</a>
                <a href="/analytics" class="nav-link" @onclick="CloseMenu">Аналитика</a>
                <AuthorizeView>
                    <Authorized>
                        <a href="/history" class="nav-link" @onclick="CloseMenu">История</a>
                        <a href="/favorites" class="nav-link" @onclick="CloseMenu">Избранное</a>
                        <button @onclick="Logout" class="btn btn-secondary btn-sm">Выйти</button>
                    </Authorized>
                    <NotAuthorized>
                        <a href="/login" class="btn btn-secondary btn-sm" @onclick="CloseMenu">Войти</a>
                        <a href="/register" class="btn btn-primary btn-sm" @onclick="CloseMenu">Регистрация</a>
                    </NotAuthorized>
                </AuthorizeView>
            </div>
        </div>
    </nav>

    <main>
        @Body
    </main>

    <BlazoredToasts Position="Blazored.Toast.Configuration.ToastPosition.BottomRight" />
</div>

@code {
    private bool menuOpen = false;

    private void ToggleMenu() => menuOpen = !menuOpen;
    private void CloseMenu() => menuOpen = false;

    private async Task Logout()
    {
        menuOpen = false;
        await AuthService.LogoutAsync();
        Nav.NavigateTo("/");
    }
}
